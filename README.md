# danielalfasi.com

Personal site — Jekyll on GitHub Pages.

```bash
bundle install
bundle exec jekyll serve
```

Posts go in `_posts/` as `YYYY-MM-DD-slug.md`. Deploy is automatic on push to `master`.
